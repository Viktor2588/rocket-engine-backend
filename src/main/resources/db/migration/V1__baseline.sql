-- V1__baseline.sql
-- Baseline migration for existing database schema (BE-070)
-- This is empty because the schema was created by Hibernate ddl-auto=update
-- Future migrations will be incremental from this point

-- Placeholder: Schema already exists from JPA auto-generation
SELECT 1;
